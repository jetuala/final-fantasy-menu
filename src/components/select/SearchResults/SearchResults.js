import React from 'react';

// Having trouble figuring out how to re-render/update this component from the SearchBar component. Might not be worth the trouble???

const SearchResults = (results) => {
    return (
        <div>
            <ul>
                
            </ul>
        </div>
    )
}

export default SearchResults;